# PasetMe Backend

Using `Gin` and `Gorm`.

## Build

```bash
$ bash dep.sh
$ go build -o pasteme_backend
```

## Test

This script will test all packages if there is no param.

```bash
./gotest [package name]
```
